services:
  - type: web
    name: chatbot-rag
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        value: your-openai-api-key
    plan: free
